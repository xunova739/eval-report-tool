# gstack Skills 使用手册

> gstack 是 YC CEO Garry Tan 开源的 Claude Code 虚拟工程团队工具包，包含 25 个斜杠命令。
> 安装路径：`~/.claude/skills/gstack`

---

## 开发流程 Skills

| 命令 | 什么时候用 | 你会看到什么 |
|------|-----------|------------|
| `/office-hours` | 有新想法，还没开始写代码，想先讨论方向 | 像产品经理一样和你过一遍方案，帮你想清楚再动手 |
| `/plan-ceo-review` | 方案写好了，想从产品/战略角度确认方向 | 模拟 CEO 视角审查计划 |
| `/plan-eng-review` | 方案写好了，想确认技术架构是否合理 | 模拟资深工程师视角审查技术决策 |
| `/plan-design-review` | 想让人看一下 UI/UX 设计是否合理 | 从用户体验角度给出设计反馈 |
| `/design-consultation` | 从零开始建设设计系统（颜色/字体/组件规范） | 帮你定义一套完整设计规范 |
| `/investigate` | 有报错或 bug，找不到原因 | 系统化排查，逐步缩小问题范围 |
| `/qa` | 功能写完，想用真实浏览器点击测试 | 打开 Chromium 模拟真实用户操作，截图存档 |
| `/qa-only` | 只想跑浏览器测试，不要 code review | 纯 QA 流程，比 /qa 更精简 |
| `/review` | 准备合并代码前，想找潜在 bug | 代码审查，明显问题自动修，其余列出 |
| `/design-review` | 想检查视觉设计（颜色/间距/对齐/响应式） | 浏览器截图对比，逐条列出设计问题 |
| `/codex` | 想要对立意见，或最严格的代码审查 | 模拟另一个工程师做对抗性 review |
| `/ship` | 功能完成，准备创建 PR | 同步代码 → 跑测试 → 创建 PR |
| `/document-release` | 发版后需要更新文档/changelog | 自动整理本次改动，更新相关文档 |
| `/retro` | 一个阶段结束后的复盘 | 分析做得好/不好/下次怎么改进 |

---

## 安全/权限 Skills

| 命令 | 作用 |
|------|------|
| `/careful` | 开启谨慎模式，操作线上/生产环境时自动加警告提示 |
| `/guard` | 最高安全级别，所有破坏性操作都需确认，限制编辑范围 |
| `/freeze` | 把编辑范围锁定在某个模块/目录，防止误改其他文件 |
| `/unfreeze` | 解除 freeze/guard 的限制 |

---

## 浏览器测试能力（/qa 底层）

调用 `/qa` 时，Claude 会控制真实的 Chromium 浏览器。支持以下操作：

### 基本流程
```
打开页面 → 识别页面元素 → 点击/填写表单 → 截图确认结果
```

### 支持的测试场景

| 场景 | 说明 |
|------|------|
| 登录/注册流程 | 自动识别输入框，填写，提交，截图验证 |
| 文件上传 | 找到上传控件，上传文件，等待成功提示 |
| 表单验证 | 提交空表单确认报错 → 填写正确数据确认成功 |
| 响应式布局 | 一键生成手机(375px)/平板(768px)/桌面(1280px)三张截图 |
| 弹窗/确认框 | 自动处理 alert/confirm/prompt 弹窗 |
| 已登录页面 | 从真实浏览器导入 Cookie，测试需要登录的页面 |
| 环境对比 | 对比 staging 和 production 的页面差异 |
| JS 错误检测 | 操作后自动检查 console 是否有报错 |

### 截图能力

```
全页截图          → 记录完整页面状态
元素截图          → 只截取某个按钮/区域
标注截图          → 截图上标出所有可点击元素（红框+编号）
响应式三图        → 三种屏幕尺寸同时截图
前后对比(diff)    → 操作前后的差异高亮显示
```

---

## 版本维护

| 命令 | 作用 |
|------|------|
| `/gstack-upgrade` | 升级到最新版本 |
| `/setup-browser-cookies` | 配置从真实浏览器导入 Cookie |
| `/setup-deploy` | 配置部署流程 |

---

## 实际对话触发示例

| 你说的话 | 自动触发 |
|----------|---------|
| "帮我测试一下上传文件的功能" | `/qa` |
| "有个报错找不到原因" | `/investigate` |
| "代码写完了，帮我看看有没有问题" | `/review` |
| "准备提 PR 了" | `/ship` |
| "想看看手机端显示效果" | `/qa` + 响应式截图 |
| "我有个新想法想讨论" | `/office-hours` |
| "上线前想做最后检查" | `/careful` + `/qa` |
| "这段代码改完想确认没有漏掉什么" | `/review` |

---

## 关闭/开启主动推荐

gstack 默认会在合适时机主动建议你使用对应命令。如果觉得打扰：

```
说："停止主动推荐"  →  Claude 会关闭，并记住这个设置
说："重新开启推荐"  →  恢复主动推荐
```
