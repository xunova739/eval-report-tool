# 标注评测报告生成工具 - 产品需求文档 (PRD)

> 版本：v1.0
> 最后更新：2026-03-24

---

## 一、产品概述

### 1.1 产品定位

**标注评测报告生成工具**是一款面向数据团队的统计分析平台，用于将标注数据转化为可读的评测报告。

**核心价值**：
- 将人工编写的口径描述自动解析为结构化统计配置
- 支持多种统计场景（单版本评测、A/B对比评测、分组统计）
- 一键生成专业的分析报告

### 1.2 目标用户

- **主要用户**：数据分析师、评测工程师
- **技术水平**：熟悉Excel操作，但不一定懂编程
- **核心痛点**：手动统计耗时长、口径理解不一致、报告编写繁琐

### 1.3 使用场景

| 场景 | 描述 |
|------|------|
| 单版本评测 | 对一批标注数据进行质量统计，生成达标率报告 |
| A/B对比评测 | 对比两个版本/模型的效果差异，判断哪个更优 |
| 分组统计 | 按类别、场景等维度分组统计指标 |

---

## 二、核心功能模块

### 2.1 数据上传与预览

**功能描述**：用户上传标注数据文件，系统进行清洗和字段分析。

**输入**：
- 支持的文件格式：Excel (.xlsx, .xls)、CSV
- 一行为一条标注记录，一列为一个字段

**输出**：
- 数据清洗报告（删除空行、列名规范化）
- 字段分布信息（每个字段的类型、唯一值列表）

**字段类型识别规则**：
| 类型 | 识别标准 | 可用运算符 |
|------|---------|-----------|
| 分类字段 | 唯一值数量 ≤ 50 | 等于、不等于、属于、不属于 |
| 多选字段 | 单元格含逗号分隔的多个值 | 包含、不包含 |
| 高基数字段 | 唯一值数量 > 50 | 为空、不为空 |
| 跳过字段 | 常量列、全唯一列、疑似人名列 | - |

**用户价值**：无需手动分析字段，系统自动识别数据结构，为后续口径配置提供基础。

---

### 2.2 口径配置

**功能描述**：定义统计指标的分子分母逻辑。

#### 2.2.1 口径解析方式

| 方式 | 适用场景 | 操作流程 |
|------|---------|---------|
| AI自动解析 | 有口径Excel或文字描述 | 上传口径文件 → AI解析 → 自动生成配置 |
| 手动配置 | 简单统计需求 | 直接添加指标和条件 |

#### 2.2.2 条件配置器

每个条件由三部分组成：
- **字段**：选择要筛选的数据列
- **运算符**：选择比较方式
- **值**：输入或选择目标值

**运算符说明**：
| 运算符 | 含义 | 示例 |
|--------|------|------|
| 等于 | 精确匹配 | 字段 = "是" |
| 不等于 | 排除匹配 | 字段 ≠ "否" |
| 包含 | 字符串包含 | 字段包含 "L2问题" |
| 不包含 | 字符串不包含 | 字段不包含 "错误" |
| 为空 | 值为空/缺失 | 字段为空 |
| 不为空 | 值不为空 | 字段不为空 |
| 属于 | 多值匹配 | 字段属于 ["A", "B"] |
| 不属于 | 多值排除 | 字段不属于 ["C", "D"] |

#### 2.2.3 分子条件逻辑

- **AND模式**：所有条件同时满足
- **OR模式**：满足任一条件即可

**示例**：
- "整体异常率"：生成失败 OR 肢体异常 OR 服装问题（OR模式）
- "高质量通过率"：已生成 AND 无异常 AND 真实度高（AND模式）

#### 2.2.4 分母配置

| 分母类型 | 说明 |
|---------|------|
| 全部数据 | 不过滤，使用所有记录 |
| 公共分母 | 所有指标共享的过滤条件 |
| 自定义分母 | 每个指标可配置独立的分母条件 |

**用户价值**：灵活的口径配置支持复杂的业务统计需求，AI解析大幅降低配置门槛。

---

### 2.3 统计计算

**功能描述**：根据口径配置计算各指标的达标率。

**计算公式**：
```
达标率 = 分子数量 / 分母数量 × 100%
```

**统计模式**：

| 模式 | 说明 | 输出格式 |
|------|------|---------|
| 普通模式 | 单版本统计 | 指标名、分子、分母、百分比 |
| 对比模式 | A/B版本对比 | 两版本各指标 + Gap + 胜负判定 |
| 分组模式 | 按维度分组 | 各分组的指标统计表 |

**实时计算**：条件变化后自动重新计算，无需手动触发。

**用户价值**：配置即计算，所见即所得，快速验证口径正确性。

---

### 2.4 Case筛选与排查

**功能描述**：查看满足/不满足特定条件的具体数据记录。

**功能点**：
- 根据口径条件筛选出具体Case
- 支持保存筛选预设（如"坏Case"、"好Case"）
- 预览筛选结果数量
- 导出筛选后的数据

**用户价值**：从统计结果下钻到具体Case，支持问题定位和数据核查。

---

### 2.5 报告生成

**功能描述**：基于统计结果自动生成分析报告。

**报告结构**：
1. **核心结论**：一句话概括整体质量 + 核心问题 + 首要建议
2. **关键洞察**：2-4条数据驱动的发现（异常、趋势、对比、瓶颈等）
3. **指标详情**：逐个核心指标分析
4. **对比分析**：（对比模式）版本胜负判定和差异分析
5. **问题归因**：基于数据推断可能原因
6. **行动建议**：按优先级排序的建议

**洞察类型**：
| 类型 | 识别方法 |
|------|---------|
| 异常发现 | 某指标显著低于/高于其他指标或基准 |
| 趋势识别 | 多个指标呈现递增/递减模式 |
| 对比差异 | 分组对比中某组明显不同 |
| 关联发现 | 某两个指标同时高/低 |
| 瓶颈定位 | 拉低整体的关键子项 |
| 优势亮点 | 表现特别好的方面 |

**用户价值**：一键生成专业报告，节省分析时间，确保报告质量。

---

### 2.6 报告导出

**支持格式**：
| 格式 | 内容 |
|------|------|
| Word (.docx) | 统计表格 + Markdown报告正文 |
| Excel (.xlsx) | 统计结果表 + 原始数据 |

**用户价值**：方便分享和归档，支持后续编辑。

---

## 三、数据流设计

### 3.1 主流程

```
用户上传数据 → 数据清洗 → 字段分析 → 口径配置 → 统计计算 → 报告生成 → 导出
```

### 3.2 口径配置流程（AI解析模式）

```
上传口径Excel → 提取口径描述 → 调用AI解析 → 获取结构化配置 → 自动修正 → 填入配置器
```

**自动修正机制**：
- 运算符修正：根据字段类型自动选择正确运算符
- 值匹配修正：使用相似度匹配纠正拼写错误
- 等级代码修正：确保等级编号与口径描述一致
- 字段名修正：匹配数据中的实际字段名

---

## 四、非功能性需求

### 4.1 性能要求

- 数据量支持：单文件 ≤ 10万行
- 统计响应：条件变化后 2秒内完成计算
- AI解析超时：≤ 60秒

### 4.2 易用性要求

- 无需编程知识，纯界面操作
- 条件编辑器支持下拉选择，无需手动输入
- 错误提示使用中文，并提供解决建议

### 4.3 兼容性要求

- 浏览器：Chrome、Edge、Safari 最新版
- 屏幕分辨率：≥ 1280×720

---

## 五、验收标准

### 5.1 核心功能验收

| 功能 | 验收标准 |
|------|---------|
| 数据上传 | 支持xlsx/xls/csv，正确识别中文列名 |
| 字段分析 | 正确识别字段类型，跳过无意义字段 |
| AI口径解析 | 解析成功率 ≥ 90%，条件值正确率 ≥ 95% |
| 统计计算 | 计算结果与Excel COUNTIFS一致 |
| 报告生成 | 报告包含数据支撑，无空泛结论 |
| 导出功能 | 导出文件可正常打开和编辑 |

### 5.2 边界场景验收

- 分母为0时显示"N/A"而非报错
- 条件为空时使用全部数据
- 字段名不存在时给出明确提示
- AI解析失败时支持手动配置降级

---

## 六、术语表

| 术语 | 定义 |
|------|------|
| 口径 | 统计指标的计算规则，包括分子分母的定义 |
| 分子 | 满足特定条件的记录数量 |
| 分母 | 统计的基数，通常是总数或满足基础条件的数量 |
| 达标率 | 分子/分母 × 100%，表示满足条件的比例 |
| GSB | Good/Same/Bad，A/B对比评测的一种分类方式 |
| 条件组 | 一组可复用的筛选条件集合 |
| 字段分布 | 每个字段的类型和值分布情况 |