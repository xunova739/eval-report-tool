# 标注评测报告生成工具 - 技术架构文档

> 版本：v1.0
> 最后更新：2026-03-24

---

## 一、架构概述

### 1.1 架构原则

- **分层设计**：领域模型层、服务层、表现层职责清晰分离
- **单一职责**：每个模块只做一件事
- **依赖倒置**：上层依赖下层抽象，不依赖具体实现
- **可测试性**：服务层可独立于UI进行单元测试

### 1.2 系统架构图

```
┌─────────────────────────────────────────────────────────────────┐
│                         表现层 (UI Layer)                        │
│  ┌─────────────────────────────────────────────────────────┐    │
│  │                      Web Application                      │    │
│  │  - 数据上传界面                                           │    │
│  │  - 口径配置编辑器                                         │    │
│  │  - 统计结果展示                                           │    │
│  │  - 报告生成与导出                                         │    │
│  └─────────────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────────┘
                                │
                                ▼
┌─────────────────────────────────────────────────────────────────┐
│                         服务层 (Service Layer)                    │
│  ┌───────────────┐  ┌───────────────┐  ┌───────────────┐        │
│  │  DataService  │  │  LLMService   │  │ ExportService │        │
│  │  数据处理服务  │  │  AI调用服务   │  │  导出服务     │        │
│  └───────────────┘  └───────────────┘  └───────────────┘        │
└─────────────────────────────────────────────────────────────────┘
                                │
                                ▼
┌─────────────────────────────────────────────────────────────────┐
│                       领域模型层 (Domain Layer)                   │
│  ┌───────────────┐  ┌───────────────┐  ┌───────────────┐        │
│  │   Condition   │  │    Metric     │  │ MetricsConfig │        │
│  │   筛选条件    │  │   统计指标    │  │   口径配置    │        │
│  └───────────────┘  └───────────────┘  └───────────────┘        │
│  ┌───────────────┐  ┌───────────────┐  ┌───────────────┐        │
│  │ MetricResult  │  │  StatsResult  │  │    枚举类型    │        │
│  │   指标结果    │  │   统计结果    │  │               │        │
│  └───────────────┘  └───────────────┘  └───────────────┘        │
└─────────────────────────────────────────────────────────────────┘
                                │
                                ▼
┌─────────────────────────────────────────────────────────────────┐
│                       基础设施层 (Infrastructure)                 │
│  ┌───────────────┐  ┌───────────────┐  ┌───────────────┐        │
│  │  配置管理器   │  │   提示词模板   │  │   工具函数    │        │
│  │ ConfigManager │  │    Prompts    │  │    Utils      │        │
│  └───────────────┘  └───────────────┘  └───────────────┘        │
└─────────────────────────────────────────────────────────────────┘
```

---

## 二、核心数据模型

### 2.1 领域模型定义

#### Condition（筛选条件）

```
Condition {
    field: string      // 字段名
    op: string         // 运算符（==, !=, contains, not_contains, in, not_in, is_empty, is_not_empty）
    value: string      // 目标值
}
```

**设计意图**：条件是所有筛选和统计的基础单元，使用结构化对象替代裸字符串，提供类型安全。

#### Metric（统计指标）

```
Metric {
    name: string                              // 指标名称
    numerator_conditions: List[Condition]     // 分子条件（AND关系）
    numerator_or_conditions: List[List[Condition]]  // OR条件组
    numerator_logic: string                   // "and" 或 "or"
    denominator_type: string                  // "common" / "all" / "custom"
    custom_denominator_conditions: List[Condition]  // 自定义分母条件
}
```

**设计意图**：
- `numerator_conditions` 存放AND条件
- `numerator_or_conditions` 存放OR条件组（每组内部AND，组间OR）
- `numerator_logic` 标识当前使用的逻辑类型

#### MetricsConfig（口径配置）

```
MetricsConfig {
    common_denominator: CommonDenominator    // 公共分母
    metrics: List[Metric]                    // 指标列表
    field_mappings: List[FieldMapping]       // 字段映射记录
    group_dimensions: List[GroupDimension]   // 分组维度
}
```

**设计意图**：完整的统计配置容器，是AI解析和统计计算之间的桥梁。

#### StatsResult（统计结果）

```
StatsResult {
    denominator_count: int           // 分母数量
    denominator_description: string  // 分母描述
    results: List[MetricResult]      // 各指标结果

    // 对比模式专用
    compare_field: string            // 对比字段
    version_a: string                // 版本A名称
    version_b: string                // 版本B名称

    // 分组模式专用
    group_field: string              // 分组字段
    groups: List[string]             // 分组值列表
    group_results: Map               // 分组结果
}
```

### 2.2 枚举类型

| 枚举 | 可选值 | 用途 |
|------|-------|------|
| OperatorType | ==, !=, contains, not_contains, in, not_in, is_empty, is_not_empty | 运算符类型 |
| FieldType | categorical, categorical_with_multi, high_cardinality, skip | 字段类型 |
| DenominatorType | common, custom | 分母类型 |
| NumeratorLogic | and, or | 分子逻辑 |

---

## 三、服务层设计

### 3.1 DataService（数据处理服务）

**职责**：数据清洗、字段分析、筛选计算、统计计算

**核心方法**：

| 方法 | 输入 | 输出 | 说明 |
|------|------|------|------|
| clean_dataframe | DataFrame | (DataFrame, Report) | 数据清洗，返回清洗报告 |
| build_field_distribution | DataFrame | Dict | 构建字段分布信息 |
| apply_condition | DataFrame, Condition | DataFrame | 应用单个条件筛选 |
| apply_conditions | DataFrame, Conditions | DataFrame | 应用多条件筛选 |
| calculate_metrics | DataFrame, MetricsConfig | StatsResult | 计算所有指标 |
| calculate_metrics_with_grouping | DataFrame, MetricsConfig, GroupDimension | StatsResult | 分组统计 |

**字段类型识别逻辑**：

```
if 唯一值数量 <= 1:
    type = "skip"  // 常量列
elif 唯一值数量 == 总行数:
    type = "skip"  // 全唯一列
elif 唯一值/总行数 > 0.8:
    type = "skip"  // 高唯一率
elif 70%样本符合中文人名模式:
    type = "skip"  // 疑似人名
elif 单元格含逗号分隔:
    type = "categorical_with_multi"  // 多选字段
elif 唯一值数量 <= 50:
    type = "categorical"  // 分类字段
else:
    type = "high_cardinality"  // 高基数字段
```

### 3.2 LLMService（AI调用服务）

**职责**：大模型API调用、JSON解析、错误处理

**核心方法**：

| 方法 | 输入 | 输出 | 说明 |
|------|------|------|------|
| call_text | prompt: string | response: string | 文本对话 |
| call_multimodal | prompt: string, images: list | response: string | 多模态调用 |
| parse_json_response | response: string | Dict | 提取并解析JSON |
| test_connection | - | bool | 测试API连接 |

**JSON解析流程**：
1. 尝试直接解析
2. 失败则尝试提取 ```json 代码块
3. 失败则尝试修复截断的JSON
4. 全部失败则抛出异常

### 3.3 ExportService（导出服务）

**职责**：Word/Excel文件生成

**核心方法**：

| 方法 | 输入 | 输出 | 说明 |
|------|------|------|------|
| export_to_word | stats_result, report_text | bytes | 生成Word文档 |
| export_to_excel | stats_result, df | bytes | 生成Excel文件 |

**Word文档结构**：
1. 标题
2. 统计表格
3. Markdown解析后的正文

---

## 四、口径解析流程

### 4.1 AI解析Pipeline

```
用户上传口径Excel
       │
       ▼
┌─────────────────┐
│  提取口径文本    │  读取Excel内容，转为结构化文本
└─────────────────┘
       │
       ▼
┌─────────────────┐
│  构建Prompt      │  注入字段分布 + 口径文本 + 规则模板
└─────────────────┘
       │
       ▼
┌─────────────────┐
│  调用AI服务      │  发送请求，获取JSON响应
└─────────────────┘
       │
       ▼
┌─────────────────┐
│  解析JSON        │  提取并解析JSON，处理截断/格式问题
└─────────────────┘
       │
       ▼
┌─────────────────┐
│  自动修正Pipeline │  多步修正确保配置正确
└─────────────────┘
       │
       ▼
┌─────────────────┐
│  返回配置        │  存入状态，触发统计
└─────────────────┘
```

### 4.2 自动修正Pipeline

```
parse_json_response
       │
       ▼
auto_fix_operators      // 根据字段类型修正运算符
       │
       ▼
auto_fix_values         // 使用相似度匹配修正值
       │
       ▼
auto_fix_level_codes    // 修正等级代码（L0/L1/L2/L3）
       │
       ▼
auto_fix_from_field_mappings  // 用field_mappings覆盖错误值
       │
       ▼
migrate_or_conditions   // 迁移OR条件结构
       │
       ▼
auto_fix_numerator_logic  // 修正numerator_logic字段
       │
       ▼
auto_fix_deduplicate    // 删除重复条件
       │
       ▼
auto_fix_invalid_fields // 移除无效字段条件
```

### 4.3 修正规则详解

#### 运算符修正

```
if 字段类型 == "categorical_with_multi":
    if 运算符 in ["==", "!="]:
        运算符 = "contains" 或 "not_contains"
    if 运算符 in ["in", "not_in"]:
        运算符 = "contains" 或 "not_contains"
```

#### 值匹配修正

```
for each condition:
    获取字段的实际值列表
    计算条件值与各实际值的相似度
    if 最高相似度 >= 70%:
        用相似度最高的值替换
```

#### 等级代码修正

```
从口径文本中提取等级编号 (L0/L1/L2/L3)
if 条件值中的等级编号与口径不一致:
    记录修正日志
    尝试从field_mappings获取正确值
```

---

## 五、统计计算逻辑

### 5.1 基础计算流程

```
1. 确定分母
   - if denominator_type == "all": 分母 = 全部数据
   - if denominator_type == "common": 分母 = 应用公共分母条件后的数据
   - if denominator_type == "custom": 分母 = 应用自定义条件后的数据

2. 计算分子
   - if numerator_logic == "and": 分子 = 应用所有AND条件
   - if numerator_logic == "or": 分子 = 应用OR条件组（组内AND，组间OR）

3. 计算达标率
   - 达标率 = 分子数量 / 分母数量 × 100%
```

### 5.2 OR条件处理

```
numerator_or_conditions 结构：
[
    [条件A1, 条件A2],  // 组A：A1 AND A2
    [条件B1],          // 组B：B1
    [条件C1, 条件C2, 条件C3]  // 组C：C1 AND C2 AND C3
]

计算逻辑：
分子 = (组A满足的数量) ∪ (组B满足的数量) ∪ (组C满足的数量)
```

### 5.3 分组统计

```
for each group_value in group_dimensions.values:
    df_group = 筛选分组字段包含group_value的数据
    对df_group执行统计计算
    记录分组结果
```

---

## 六、状态管理

### 6.1 Session状态设计

| 状态键 | 类型 | 说明 |
|--------|------|------|
| df | DataFrame | 当前加载的数据 |
| field_distribution | Dict | 字段分布信息 |
| parsed_metrics | Dict | AI解析的原始配置 |
| editing_metrics | Dict | 当前编辑中的配置 |
| confirmed_metrics | Dict | 已确认的配置 |
| stats_result | Dict | 统计结果 |
| report_text | string | 生成的报告 |

### 6.2 状态流转

```
上传数据 → df, field_distribution
解析口径 → parsed_metrics → editing_metrics → confirmed_metrics
编辑配置 → editing_metrics
确认配置 → confirmed_metrics → 触发统计 → stats_result
生成报告 → report_text
```

---

## 七、扩展点设计

### 7.1 新增统计模式

1. 在领域模型层添加新模式相关的数据结构
2. 在DataService中添加对应的计算方法
3. 在UI层添加新模式的选择和配置入口

### 7.2 新增导出格式

1. 在ExportService中添加新的导出方法
2. 在UI层添加导出按钮

### 7.3 新增AI解析场景

1. 在提示词模板中添加新的Prompt模板
2. 在解析Pipeline中添加场景分支

---

## 八、错误处理策略

### 8.1 异常层次

```
DomainException (基类)
├── ValidationError     // 验证失败（字段不存在、值无效等）
├── ConfigLoadError     // 配置加载失败
├── DataServiceError    // 数据服务失败
│   └── DataFilterError // 筛选失败
└── ExportError         // 导出失败
```

### 8.2 错误处理原则

1. **用户友好**：错误信息使用中文，提供解决建议
2. **可恢复**：尽量降级处理而非直接报错
3. **可追溯**：记录详细日志便于排查

---

## 九、性能优化策略

### 9.1 数据处理优化

- 使用向量化操作替代循环
- 条件筛选使用Pandas原生方法
- 大数据集分批处理

### 9.2 UI响应优化

- 统计计算异步执行
- 条件变化防抖处理
- 缓存中间计算结果

### 9.3 AI调用优化

- 复用HTTP连接
- 设置合理超时
- 失败自动重试

---

## 十、部署架构

### 10.1 单机部署

```
┌─────────────────────────────────────┐
│           应用服务器                 │
│  ┌─────────────────────────────┐    │
│  │     Web Application         │    │
│  │     (Port: 8502)            │    │
│  └─────────────────────────────┘    │
│  ┌─────────────────────────────┐    │
│  │     本地文件存储             │    │
│  │     (temp/)                 │    │
│  └─────────────────────────────┘    │
└─────────────────────────────────────┘
              │
              ▼
       ┌─────────────┐
       │  LLM API    │
       │  (外部服务)  │
       └─────────────┘
```

### 10.2 运行要求

- 内存：≥ 2GB（处理10万行数据）
- CPU：≥ 2核
- 存储：≥ 1GB（临时文件空间）